{
  "name": "dontyoufillit-web",
  "version": "1.0.0",
  "description": "DontYouFillIt is a Breakout-style game mixed with snooker.",
  "private": true,
  "scripts": {
    "build-js": "npx esbuild --bundle --outfile=www/app.js --sourcemap app.js",
    "build-watch": "npm run build-dev -- --watch",
    "build": "make"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Sigill/dontyoufillit-web.git"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.25.5"
  },
  "devDependencies": {
  }
}
